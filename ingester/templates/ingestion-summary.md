# Ingestion summary: {{TID}}

Version 0.1, {{DATE}}.

Generated by `ingester/bin/summary` from the engagement's files and the Claude Code session files. Do not edit; rerun `stage {{ENG}} {{TID}} summary` to regenerate.

- Transcript: {{TID}} ({{FILE}})
- Session date: {{SESSION_DATE}}
- Model: {{MODEL}}
- Ingester version: {{INGESTER_VERSION}}
- Rules version: {{RULES_VERSION}}
- Claude Code sessions: {{SESSIONS}}
- Window: {{WINDOW}}
- Compared with: {{PREVIOUS}}

## The transcript

{{TRANSCRIPT}}

## Time

Elapsed runs from the first `/ingest-transcript` command for this transcript to the end of S3, taken from the S3 run log. Active is the time the model and the scripts were working: every gap between two events in the session of under ten minutes, except a gap that ends in a reviewer message, plus the whole of the S3 write, which runs in the background and can take half an hour on a large dossier. Waiting is every gap that ends in a reviewer message. Idle is the remaining gaps of ten minutes or more.

{{TIME}}

Stage times, from the run logs under `logs/` (UTC):

{{STAGES}}

## Tokens

Counted once per model call, by message id, from the assistant lines in the session files within the window. Cache read is context re-read from the prompt cache on each call; cache write is context newly cached; input is uncached input.

{{TOKENS}}

## Cost

What the same tokens would cost at Anthropic's first-party API list prices, from `ingester/pricing.tsv`. Each model call is priced by its own model, cache writes by their TTL (5-minute or 1-hour, as the session file records them) and fast-mode calls at the fast multiplier. On a Claude subscription plan this is an equivalent, not a charge; the share of the plan's usage limit is not in the session files, and `/usage` in the session shows it.

{{COST}}

## Output

{{OUTPUT}}

## Accuracy

The reviewer's verdicts on the drafted dossier, which stand in for accuracy until a reference is marked. Changed means an Edit or a Reject. A Confident item that was changed is the design's Confident-but-wrong count (Q4) and should be zero.

{{ACCURACY}}

{{RUN_REPORT}}

## Compared with the previous transcript

{{COMPARISON}}

## Caveats

{{CAVEATS}}

## Metrics

Read by the next transcript's summary for the comparison. One `- key: value` per line.

{{METRICS}}
